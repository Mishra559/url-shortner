package com.urlshortener.config;

// Redis caching is disabled in this build.
// To enable Redis: add spring-boot-starter-data-redis to pom.xml
// and set spring.cache.type=redis in application.properties.
public class RedisConfig {
    // intentionally empty
}

